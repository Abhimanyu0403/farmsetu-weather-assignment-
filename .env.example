# Django
SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
CSRF_TRUSTED_ORIGINS=http://127.0.0.1:8000,http://localhost:8000

# Local PostgreSQL (used by Docker Compose)
DB_NAME=weather_db
DB_USER=weather_user
DB_PASSWORD=weather_password
DB_HOST=db
DB_PORT=5432

# Railway PostgreSQL (set automatically by Railway — do not set locally)
# PGDATABASE=
# PGUSER=
# PGPASSWORD=
# PGHOST=
# PGPORT=
